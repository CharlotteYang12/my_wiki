# LLM Wiki Agent Guide

这个仓库是一个由 LLM 维护的个人知识库。`llm-wiki.md` 描述理念，`AGENTS.md` 记录本工程的实际操作规则。

## 目录约定

- `raw/`：原始资料层，原则上只读。用户放入的阅读列表、网页剪藏、原始笔记和附件都保留在这里。
- `raw/assets/`：原始资料附件目录，尽量不要在整理 wiki 时改动。
- `wiki/`：LLM 维护的知识页目录。论文笔记、行业资料、方法总结和维护页都放这里。
- `wiki/assets/`：wiki 页面引用的图片统一放这里。
- `wiki/index.md`：简化总索引，按大类列出页面和摘要。
- `wiki/log.md`：中文维护日志，按时间追加记录。
- `.obsidian/`：Obsidian vault 配置，当前启用 Dataview 和 Marp 插件。

## 写作语言

- 默认用中文写正文、摘要、日志和索引。
- 论文名、模型名、数据集名、方法名、公式符号和必要英文术语保留原文。
- 表达优先简洁，避免长篇复述原文。
- 用户要求“精炼”时，应直接压缩成要点或表格，而不是保留大段解释。

## Wiki 页面格式

普通 wiki 页面建议使用以下结构：

```md
> 一句话核心结论（可选）

# 背景

# 方法

# 实验结果 / 实验结论

## 索引信息

> 类别：论文笔记 / 方向 / 子方向  
> 索引标签：#标签1 #标签2 #标签3
```

要求：

- `## 索引信息` 放在页面末尾，不放在开头。
- 一级标题根据页面内容确定，常见为 `# 背景`、`# 方法`、`# 实验结果` 或 `# 实验结论`。
- 子标题从 `##` 开始，细分小节用 `###`。
- 不要为了“规范”强行大改用户已有笔记结构；优先做局部整理。

## 图片规则

- `wiki/` 页面引用的图片必须放在 `wiki/assets/`。
- Markdown 图片路径使用相对路径：`![](assets/文件名.png)`。
- 如果发现图片在 `wiki/` 根目录，先移动到 `wiki/assets/`，再修正引用。
- 移动图片后要确认：
  - Markdown 中不再引用根目录图片。
  - `wiki/assets/文件名` 实际存在。
  - 不误删用户已有图片。

## 索引规则

`wiki/index.md` 使用简洁分类表格，不写标签，不写图片资源。

当前大类包括：

- `Agent`
- `强化学习与世界模型`
- `机器人控制`
- `视觉生成`
- `自动驾驶安全`
- `其他资料`

每个大类使用两列表格：

```md
| 论文 | 摘要 |
| --- | --- |
| [[页面名]] | 一句话摘要。 |
```

如果不是论文，也可以用 `页面` 作为第一列名。

新增或整理页面时必须更新 `wiki/index.md`：

- 新论文页按主题放入对应大类。
- 没有合适大类时可新增大类，但要保持简洁。
- 摘要写一句话，说明核心内容和价值。

## 日志规则

每次摄取新页面、整理索引、移动图片、批量维护时，都要追加 `wiki/log.md`。

格式：

```md
## [YYYY-MM-DD] 类型 | 简短标题

- 做了什么。
- 更新了哪些页面。
- 是否移动图片、更新索引、补索引信息。
```

日志用中文。不要改写旧日志，除非用户明确要求。

## 摄取新资料流程

当用户要求“更新 wiki”“按之前要求生成 LLM Wiki”“整理某篇笔记”时：

1. 读取目标页面、`wiki/index.md`、`wiki/log.md`。
2. 清理图片：把目标页面引用的图片移动到 `wiki/assets/`，并修正引用。
3. 整理标题层级：保留用户指定的一级标题；子标题从 `##` / `###` 组织。
4. 在页面末尾补 `## 索引信息`。
5. 更新 `wiki/index.md`，把页面放入合适大类。
6. 更新 `wiki/log.md`。
7. 检查图片引用和 linter。

## 维护偏好

- 保留用户的原始理解和口吻，不要把笔记改成论文综述腔。
- 优先压缩重复内容，合并表格，补清楚逻辑顺序。
- 对用户正在看的文件做最小必要修改。
- 不要移动 `raw/` 内容，除非用户明确要求。
- 不要自动提交或推送 Git，除非用户明确要求。

## Git 规则

- 用户要求“推 Git / 更新到 Git”时再提交。
- 提交前先看 `git status`、`git diff`、最近提交信息。
- 这个仓库推送 GitHub 时通常需要临时代理：

```powershell
git -c http.proxy="http://172.22.0.88:8080" -c https.proxy="http://172.22.0.88:8080" push origin main
```

- 不要改全局 git 配置。
